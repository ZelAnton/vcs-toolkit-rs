/target

# Local agent task-tracking workspace — never committed.
/.work/

# Per-user scratch notes — never committed. Use these names freely for
# throwaway planning/working files.
*.local.md
task_plan.md
findings.md
progress.md

# Rust
**/*.rs.bk
*.pdb

# Nix
/result
