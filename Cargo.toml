[workspace]
resolver = "3"
members = [
    "crates/diff",
    "crates/cli-support",
    "crates/git",
    "crates/jj",
    "crates/github",
    "crates/gitlab",
    "crates/gitea",
    "crates/forge",
    "crates/core",
    "crates/testkit",
    "crates/watch",
    "crates/mcp",
]
# `fuzz/` is a cargo-fuzz crate (nightly + libFuzzer); excluded so it never
# touches the stable build, MSRV, or the CI gate. Run it manually — see
# fuzz/README.md.
exclude = ["fuzz"]

# Metadata shared by every crate in the toolkit. Each crate inherits these via
# `field.workspace = true` in its own Cargo.toml. NOTE: `version` is *not* here
# on purpose — the workspace crates publish independently and carry their own
# version (see each crate's [package]).
[workspace.package]
edition = "2024"
# Minimum supported Rust: edition 2024 needs 1.85, but the wrappers use
# let-chains (stabilised in 1.88), so 1.88 is the real floor. Declared here (and
# inherited via `rust-version.workspace = true`) so the contract is machine-checked
# — `cargo build` on an older toolchain fails early with a clear message.
rust-version = "1.88"
license = "MIT"
repository = "https://github.com/ZelAnton/vcs-toolkit-rs"
homepage = "https://github.com/ZelAnton/vcs-toolkit-rs"
# Note: `documentation` is intentionally NOT set at workspace level. Each published crate
# relies on crates.io/docs.rs auto-linking to its own per-crate documentation page,
# avoiding the fragility of maintaining explicit per-crate documentation URLs that could
# drift from the crate's actual published name.
authors = ["Anton Zhelezniakou"]

# ---------------------------------------------------------------------------
# Workspace dependencies
#
# Convention (see CONTRIBUTING.md "Dependency management"): this workspace
# fixes NO allow-list of crates. Declare a dependency here once and reference
# it from a member with `<crate>.workspace = true` when more than one crate
# needs it.
# Give every entry an inline comment explaining *why* it's here. Pin major
# versions, enable only the features used, and keep Cargo.lock committed.
# ---------------------------------------------------------------------------
[workspace.dependencies]
# External job-backed process launcher (Windows Job Object / Linux cgroup v2, so a
# git/jj/gh subprocess tree dies on close), with the `CliClient` core, the
# `cli_client!` macro, structured `Error`, and the `ScriptedRunner`/`RecordingRunner`
# test seams the wrappers build on (under `processkit::testing`). Was the internal
# `vcs-process` crate; now the published `processkit`. Pin `"3.1"`: the 3.x line is
# semver-stable, so the caret spans future 3.x releases and `cargo update` pulls them
# safely; it carries forward everything the 2.x pin was kept for (the `Error`
# constructors `Error::spawn`/`not_found`/`stdin`/`parse`, the Windows
# process-group/PATHEXT fixes, stdin validation, bounded output buffers, stdin-retry
# and stderr draining, and the stale Windows CTRL_BREAK-leader cleanup), which matter
# because the wrappers resolve and launch user-installed CLIs while `vcs-watch`
# manages job-object/process-group lifecycles. What 3.0 breaks, and how this
# workspace answers it:
#   * `Error` is now an opaque struct wrapping a boxed `ErrorReason` (the former enum,
#     variants and fields unchanged). Reads still go through the accessors
#     (`program()`, `code()`, `stdout()`/`stderr()`, `is_timeout()`, …); variant
#     matching moved to `err.reason()` / `err.into_reason()`, and coarse classification
#     to the flat `err.kind() -> ErrorKind`. Both `ErrorReason` and `ErrorKind` are
#     re-exported from every wrapper crate that re-exports `Error`, so downstream users
#     keep the ability to classify a failure through our surface.
#   * The output-event stream was renamed with no deprecated aliases
#     (`output_events()`→`events()`, `OutputEvent`→`ProcessEvent`,
#     `OutputEvents`→`ProcessEvents`) and now has to be joined via its `finish()`
#     finisher. This workspace drove no such stream when 3.0 landed, and does now:
#     `vcs_cli_support::run_with_progress_within` polls `events()` concurrently with
#     `finish()` and re-exports `ProcessEvent`. It was written against the post-3.0
#     names and the finisher join from the start, so nothing here carries a 2.x
#     spelling.
#   * Output-limit accounting switched from decoded line-content bytes to raw bytes read
#     from the pipe. This workspace does set byte caps — `vcs_cli_support::OutputBudget`
#     projects onto `OutputBufferPolicy::max_bytes`. Audited site by site (T-130), the
#     effect splits by stream, and the split is what `OutputBudget::bytes`' doc now
#     states: the RAW-stdout capture every content verb reads through
#     (`run_untrimmed` -> `output_bytes`) was counted in raw pipe bytes before 3.0 as
#     well — `pump_raw_bytes`/`push_capped_bytes` are byte-identical between 2.3.2 and
#     3.0.0 — so no content ceiling (the 64 KiB test caps, the MCP `--max-output-bytes`
#     default) moved; the LINE-pumped streams, i.e. a content verb's stderr under the
#     same fail-loud policy, now charge every line terminator, so a cap set against the
#     old unit trips one byte per line earlier there. Both boundaries are pinned by
#     `vcs_cli_support`'s `content_budget_*` tests. The drop-oldest diagnostic
#     projection (`clone`/`fetch`, `gh run watch`) is unaffected either way: drop-mode
#     retention is still bounded by decoded line-content bytes.
#   * `OverflowMode::DropNewest` now keeps a true contiguous prefix. Not applicable: this
#     workspace never selects that mode — `OutputBudget` projects only onto `Error`
#     (content) and `DropOldest` (the default of `unbounded()`/`bounded()`, diagnostics);
#     rg finds no `DropNewest` at all, and the only two `with_overflow` calls both select
#     `Error`. The content path also refuses a truncated capture outright rather than
#     parsing a partial one, so no parser here can depend on how a head is retained.
#   * Windows ConPTY launches now clear inherited standard handles. Not applicable: the
#     fix lives behind processkit's non-default `pty` feature, which no crate here
#     enables (only `mock`, `tracing`, `record` are), and nothing in this workspace calls
#     `use_pty`/`pty_size`/`resize_pty` — every child is spawned on the ordinary piped
#     backend, so no capture or parsing path can depend on the old handle behaviour.
#   * 3.1 adds an output-inactivity watchdog. `vcs-github` applies it only to
#     `gh run watch`: the command normally refreshes its table every ~3 seconds, so a
#     five-minute quiet period detects a stalled watcher without bounding legitimate
#     multi-hour CI. The other new 3.1 controls (CPU affinity and named-pipe readiness)
#     are intentionally left opt-in for callers: VCS wrappers must not impose scheduler
#     policy or assume that a command is a local Windows service.
# What 3.2 adds, and what this workspace takes from it: nothing this build compiles
# behaves differently, so the bump is a lockfile refresh rather than an adoption. Audited
# against the published 3.2.0 crate source, not the release notes: `buffer.rs`, `pump.rs`,
# `stdin.rs`, `lookup.rs`, `retry.rs`, `doubles.rs` and `cassette.rs` are byte-identical to
# 3.1.0 — so no output-byte accounting unit, `PATH`-search algorithm, retry, mock or
# cassette behaviour moved — and the crate's own `public-api.txt` grows by 193 lines with
# none removed or changed. Per audited area:
#   * Executable resolution and environment. The spawn-free preflight and the post-spawn
#     `NotFound` enrichment now resolve a bare name against the command's *effective
#     child* `PATH` (one shared `resolution_path_source()`) instead of skipping the search
#     whenever a command relocates `PATH`. Not applicable: nothing here relocates a child
#     `PATH` — every `env`/`env_remove` sets a `GIT_*`/`JJ_CONFIG`/`LC_ALL`/token variable,
#     and there is no `env_clear`/`inherit_env` anywhere — so `customizes_path()` is false
#     for every command this workspace builds and both versions take the same
#     `PathSource::ProcessPath` branch.
#   * Supervision, readiness and pipelines. The typed `SupervisionEvent` lifecycle stream,
#     the `Supervisor` session, opt-in per-stage stderr merging (`merge_stderr_in_pipe`),
#     and the pipeline teardown-drain/salvage fixes all sit on surfaces this workspace
#     never names: it runs one child at a time through `ProcessRunner`/`CliClient` and
#     builds no `Pipeline`, and `vcs-watch` owns no `ProcessGroup` of its own — its bounded
#     re-query relies on `JobRunner`'s kill-on-drop group, reached through `vcs-core`.
#     Verdict for those pipeline/supervision surfaces: **does not affect us**; no new
#     feature is enabled.
#   * Ordinary cancellation, finish, and late exit observation are a separate verdict.
#     Processkit 3.3 documents first-observation-wins for `Command::cancel_on`: a token
#     that fires after an observed reap cannot rewrite it, while a token fired before the
#     first finisher observes an already-exited child still reports `Cancelled`. The
#     `running` changes add crate-private exit-observation seams for buffering captures
#     and bounded stdin finalization for borrowed waits; `runner` keeps the ordinary
#     single-child path unchanged and only routes the opt-in merged pipe through the new
#     reader. This contract does reach our concrete call sites: `ManagedClient::default_cancel_on`
#     (`crates/cli-support/src/lib.rs:1639-1647`) supplies the token, and
#     `run_with_progress_within` (`:195-222`) starts the run, consumes `events()`, and
#     joins `finish()`. Existing cancellation coverage exercises this path through
#     `vcs-git`/`vcs-github`/`vcs-jj`; the T-162 regression test beside those streamed
#     tests also fires the token after a scripted child has exited but before the joined
#     finisher makes its first observation. Verdict: **affects us at the audited
#     cancellation contract**, but no wrapper API or call-site change is required.
#   * Graceful cancellation ladder. `Command::cancel_grace`/`cancel_signal` route a fired
#     `cancel_on` token through the same soft-signal → grace → hard-kill ladder as
#     `timeout_grace`/`timeout_signal`. Deliberately not adopted: left unset (the default)
#     a cancellation is still an immediate whole-tree kill, and the runner's teardown bound
#     is `grace.max(cancel_grace.unwrap_or(ZERO))` — unchanged without the knob. The
#     clients here expose `default_cancel_on` so a *caller* supplies the token, and the
#     `fetch`/`clone` verbs keep their own `timeout_grace`; how long a cancellation should
#     wait before killing is consumer policy, the same reason 3.1's controls stayed opt-in.
#   * Windows. The ConPTY fixes and the PTY terminal-EOF fix stay behind the non-default
#     `pty` feature (they live in the PTY launch path), which no crate here enables — the
#     same answer the 3.0 ConPTY note records. Two Windows changes do land on the ordinary
#     piped backend and are taken as they are: a freshly contained child's threads are now
#     resumed by a per-process `NtGetNextThread` walk, with the system-wide Toolhelp
#     snapshot kept as the fallback, cutting fixed start cost with containment unchanged;
#     and every ProcessKit child creation now serializes on one process-global spawn mutex
#     so an ordinary spawn cannot observe ConPTY's standard-handle swap. That mutex is held
#     across `CreateProcess` only — never across an await, never for a child's lifetime —
#     so concurrently running children still overlap.
#   * The one upstream change that is not compiler-caught: POSIX
#     `ProcessGroup::suspend`/`resume` now report a genuine `SIGSTOP`/`SIGCONT` delivery
#     failure as `ErrorReason::Io`. Not applicable — nothing here constructs or drives a
#     `ProcessGroup` directly.
#   * The new opt-in surfaces are all declined, and no new cargo feature is enabled:
#     `report-serde` and `json` (report serialization, `output_json`, NDJSON `JsonLines`)
#     — these crates own their parse contracts and already hand back typed values, and the
#     forge wrappers decode CLI output through their own `parse` modules; `wait_for_path`
#     and the HTTP status probe — service-readiness probes, while a VCS command is run to
#     completion rather than waited on for a pidfile or a port; `arg0` and the typed Unix
#     `rlimit` builders — multicall and resource policy belong to the host application, not
#     to a wrapper; `limit_evidence`/`LimitVerdict` (behind `limits`) and the FreeBSD
#     process-reaper containment — this workspace sets no resource caps and ships no
#     FreeBSD-specific path.
# At BASE the requirement deliberately stayed `"3.1"`: nothing here used a 3.2-only API,
# so raising the floor would constrain downstream resolution for no gain. The caret spans
# the 3.x line and the BASE lock pinned 3.2.0.
# What 3.3 adds, and what this workspace takes from it: the lock now resolves 3.3.0, but
# the floor remains `"3.1"` and no 3.3-only feature is enabled. The published 3.2.0 and
# 3.3.0 source trees were compared from Cargo's registry cache; each changed runtime area
# was then checked against the BASE call sites below:
#   * `pump`: 3.3's live partial-tail salvage/sealing changes are internal to the line
#     sinks. The only production stream consumer here is
#     `crates/cli-support/src/lib.rs:196-222` (`events()` joined with `finish()`), while
#     raw stdout uses `:1849-1861` (`output_bytes`). No local code uses Pipeline's
#     `LineCapture`/`RawCapture` snapshot path. The existing `content_budget_*` tests
#     (`crates/cli-support/src/lib.rs:2800-2945`) still pin raw stdout and line-pumped
#     stderr at exact byte boundaries; verdict: **does not affect us**.
#   * `doubles`: `ScriptedRunner`/`ScriptedProc` now frame output with each command's
#     terminator instead of `str::lines()`. BASE uses the doubles at
#     `crates/cli-support/src/lib.rs:1952`, `crates/github/src/lib.rs:2465`, and
#     `crates/watch/src/lib.rs:1167`, but no workspace command calls `line_terminator`;
#     all hermetic consumers stay on the default newline framing. Verdict: **does not
#     affect us**; the full workspace hermetic suite is the check for this boundary.
#   * `cassette`: replay now accepts versions `1..=4` and recording has a dedicated
#     non-piped-stdout path. Both committed fixtures are already version 4; replay is
#     exercised by `crates/github/src/lib.rs:4304-4344`, and the only record writers are
#     `crates/github/tests/cli.rs:159`/`:177`, each using the default piped
#     `JobRunner`. Verdict: **does not affect us**; no cassette or test change is needed.
#   * `command`: PTY/merge/line-framing controls remain opt-in — BASE has no `use_pty`,
#     `pty_size`, `resize_pty`, `merge_stderr_in_pipe`, or command-level `line_terminator`
#     call site — but the ordinary `cancel_on` first-observation contract is used by the
#     `default_cancel_on`/`events()` + `finish()` path described above. Verdict: **affects
#     us for cancellation semantics; does not affect us for the opt-in I/O knobs**.
#   * `runner`: 3.3's new shared-pipe reader is only selected by
#     `merge_stderr_in_pipe`; BASE has no such call and constructs no `Pipeline`. The
#     regular `ProcessRunner` path remains the same. Verdict: **does not affect us**.
#   * `running`: `LineCapture::snapshot`/`RawCapture` and the exit-observer plumbing serve
#     Pipeline timeout attribution/salvage; BASE constructs no `Pipeline`, but the shared
#     `finish_lines`/`finish` observation point is exercised by the single-run
#     `run_with_progress_within` path at `crates/cli-support/src/lib.rs:195-222`.
#     The wrapper-level regression pins that an already-exited scripted run cancelled
#     before its first `finish()` observation remains `Cancelled`; the existing wrapper
#     tests pin mid-run cancellation and cleanup/fresh-token behavior. Verdict: **affects
#     us for the ordinary finish/cancellation contract; no code migration is needed**.
#   * `error`: the 3.3 error-path changes are for the new process-group/control paths;
#     local matches at `crates/cli-support/src/lib.rs:1077-1193` and `:1977-2165`
#     classify ordinary wrapper command results and do not construct those groups.
#     Verdict: **does not affect us**.
#   * `group` and `stats`: `ProcessGroup::adopt_external` and
#     `ProcessGroupStats::{io_read_bytes,io_write_bytes,peak_process_count}` are new
#     process-group surfaces. BASE contains no ProcessGroup construction or stats
#     access; `vcs-watch` watches repository files and delegates process execution to
#     `JobRunner`. Verdict for both: **does not affect us**; no new feature is enabled.
# The 3.3 byte-identity claims recorded for 3.1→3.2 therefore do **not** carry forward:
# `pump`, `doubles`, and `cassette` changed upstream and were audited separately here.
# No wrapper output, cassette schema, byte limit, or public wrapper signature changed,
# so no user-facing `[Unreleased]` entry is warranted. The cancellation call sites were
# nevertheless covered because the 3.3 audit changes the documented first-observation
# boundary even though the workspace keeps the same API calls.
# The requirement deliberately stays `"3.1"`: no 3.3-only API is used, and raising the
# floor would constrain downstream resolution for no gain. `Cargo.lock` is the only
# dependency-state change; unrelated `windows-sys` consolidation was reverted after the
# scoped update because processkit 3.3 does not require it.
processkit = "3.1"

# Auto-generated Mock*Api (git, jj, github, gitlab, gitea) for consumers' async-trait tests.
# Unified version: 0.15 satisfies both wrapper crate requirements (was 0.13) and processkit/mock needs.
mockall = "0.15"

# Statistical benchmarking harness for the large, pure parser fixtures in vcs-diff,
# vcs-git, and vcs-jj. Dev-only: benchmark binaries are never published dependencies.
criterion = "0.8"

# ---------------------------------------------------------------------------
# Workspace lints — inherited by every crate via `[lints] workspace = true`.
# ---------------------------------------------------------------------------
[workspace.lints.rust]
# Teach the compiler the `docsrs` cfg: docs.rs (and our nightly doc build) pass
# `--cfg docsrs` via each crate's `rustdoc-args` to switch on the `#[doc(cfg(...))]`
# "Available on crate feature `x`" badges; without this, `cfg(docsrs)` would warn
# as an unexpected cfg under `-D warnings`.
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(docsrs)'] }
