# Won't do — consolidated register

> **Status:** decision record / closed. These are scope boundaries and settled
> rejections. The first group migrated here from `ROADMAP.md`'s former "Deliberately out
> of scope" and "Consciously rejected" lists (so the roadmap holds only live work); the
> last group are new verdicts from the 2026-06-09 development sweep. Recorded so they
> aren't re-proposed. A concrete consumer with a **new argument** can reopen any of them
> (move its substance to [`../ideas/`](../ideas/)).

## Out of scope (boundaries — belong to a different layer)

| # | Candidate | Why not |
|---|---|---|
| W1 | Copy-on-write worktree cloning (reflink) + its cross-process lock | Stays in `agent-workspace`: the copy strategy is injected by the consumer, and `reflink-copy` is not a toolkit dependency. The toolkit's seam is `worktree_add(…, no_checkout)`, which already exists. |
| W2 | A single cross-backend `merge`/`undo` button on the facade | git merge vs jj `new_merge`+`squash`, and git history rewriting vs jj's op log, diverge for real; §2 exposes honest per-backend primitives instead. |
| W3 | A blocking (non-async) API | Both consumers run tokio; the only synchronous need is `Drop`-context cleanup, which the `blocking` helper modules already cover. |
| W4 | Index-repair / batching policy after `--no-checkout` | Application policy (progress UI, thresholds), not a CLI-wrapping primitive. |

## Consciously rejected (design decisions — don't re-litigate)

| # | Candidate | Why not |
|---|---|---|
| W5 | Codegen / templating across the five wrappers | Heavy machinery vs the AGENTS.md builder convention, for ~150 lines. |
| W6 | A `Backend` trait instead of the enum | The enum + free-fn modules *is* the adapter, monomorphised. |
| W7 | Trait-only `Repo` (drop the inherent API) | Kills rustdoc / ergonomics. |
| W8 | Broad serde-JSON proptests | serde doesn't panic. *(Does NOT cover the roadmap's gitea-parser hardening — that proptests the **post-serde shaping** logic, which genuinely can panic.)* |
| W9 | Windows/macOS **integration** lanes (the `--ignored` real-binary suites) | Cost; revisit separately. *Flagged: of all rejected items this is the one most worth a future re-look, given the toolkit's explicit Windows-correctness focus — but not now. (The hermetic `test` job already runs 3 OSes.)* |
| W11 | `mockall::automock` on the facade traits (`VcsRepo`/`ForgeApi`) | Incompatible with their `macro_rules!`-generated `:ty` signatures (§7.3 spike, reproduced). Seam-testing over a fake `ScriptedRunner` is the supported path. |
| W12 | Sharing the conflict marker-scanner across vcs-git/vcs-jj | Disjoint marker grammars (`<=>|` vs `<%+->`); extraction bends one model. |
| W13 | Per-command `Command::cancel_on` in the object-safe `*Api` traits | The client-level `default_cancel_on` (processkit 0.8) is the ergonomic, mock-friendly seam (§6.13). |

> **W10 — retry jitter — reopened.** Previously rejected as "needs processkit support";
> it is now an **active upstream proposal** to ProcessKit-rs (fixed 500 ms fetch backoff
> → thundering herd at `agent-workspace` fan-out scale). Tracked via `.hq/comms`
> (`T-20260609-vcs-processkit-feedback`). Not a vcs-toolkit-local item.

## New verdicts (2026-06-09 sweep)

| # | Candidate | Why not |
|---|---|---|
| W14 | Extract `facade_trait!` into a shared `macro_rules!`-only crate | Net-negative at only **2** call sites (`vcs-core`, `vcs-forge`): a new published+versioned crate adds publish-order + dependency-layer cost for negative net lines. The duplication is deliberate and documented in both copies. Reopen only if a **3rd facade** appears (e.g. a new-VCS-backend facade) — see `ideas/later-new-backends.md`. |
| W15 | A dynamic / probing forge-capability matrix | The static `supports(op) -> bool` proposed in `ideas/next-forge-surface.md` is sufficient for ~4 facts; a runtime probe is over-engineering. |
| W16 | A standalone external docs **site** (mdBook / GitHub Pages) | The `include_str!`-embedded guides + per-crate `docs/` already render the full guide set on docs.rs and stay in sync with the code; a separate site is a maintenance fork with no added reach. |

## Resolved by analysis (2026-06-10 roadmap wave)

- **R8 — adopt `Command::ok_codes` — not worth it.** processkit 0.9.1 added `ok_codes`
  (treat a known non-zero exit as *success* for the checking verbs `run`/`run_unit`).
  vcs-toolkit has **no site that needs it**: 0/1 answers use `.probe()`
  (`diff_is_empty`, `branch_exists`, the range/staged checks), and the auth bool uses
  `.exit_code()` (github/gitlab) or `.output_string()` + JSON (gitea) — all already idiomatic
  and clearer than `run()` + `ok_codes(...)`. Converting them would be a step backwards.
  Recorded so it isn't re-attempted; reopen only if a real site wants a non-zero exit to
  count as success for `run()`/`run_unit()`. (R9 `is_transient` and R10 `timeout_grace`
  from the same 0.9.1 surface *were* adopted — see `ROADMAP.md`.)

## Note

The throughline: vcs-toolkit is a **typed, thin CLI wrapper** over the *installed*
git/jj/gh/glab/tea binaries — it reflects each tool's exact behavior, config, and
credentials, and stays honest about where the backends genuinely diverge (the facades are
a deliberate least-common-denominator, not a false unification). Features that erode that
identity — a blocking API, a false cross-backend button, in-process object access (that's
`gitoxide`/`git2`, see `docs/positioning.md`) — stay out, even when tempting.
